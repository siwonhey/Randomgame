# BLITZ — UI 중심 요약 PRD
팽이들이 겨루며 마지막까지 남은 승자가 당첨되는 랜덤 추첨 게임.

## 1. 핵심 기능

- **랜딩 스플래시**: 페이지 최초 진입 시 로고 영상 1회 재생 후 자동 디스미스 (재시작 시에는 미재생)
- **참가자 입력**: 텍스트 입력으로 참가자 추가 (Enter 분리, 쉼표/탭/줄바꿈 페이스트 일괄 등록), 최대 30명
- **자동 색상 배정**: 참가자별 컬러 자동 할당 (총 13색 순환)
- **셔플**: 참가자 순서 및 색상 무작위 재배정 (idle 상태에서만)
- **타이틀 입력**: 이벤트 명 (필수, 미입력 시 START 차단 + 경고 표시)
- **시작 검증**: 최소 참가자 2명 + 타이틀 입력 시에만 START GAME 활성화
- **배틀 시퀀스**: intro(시네마틱 카메라) → countdown(3·2·1·GO) → battle(물리 시뮬레이션, 30초 타이머)
- **실시간 HUD**: 잔여 인원, 남은 시간(10초 이하 urgent 색상), 타이틀 표시
- **퇴장(elimination)**: 링 아웃 시 즉시 순위 기록, 남은 인원 카운트 갱신, 마지막 1인 잔존 시 종료
- **일시정지**: 게임 화면 클릭 또는 설정 팝업 오픈 시 자동 pause, 재개 시 resume
- **인게임 편집 팝업**: 기어 아이콘으로 setup 패널을 모달로 재활용, 배경 dim + 자동 pause
- **결과 화면**: 우승자 3D 표시, 우승자 이름, 2위 이하 순위 리스트, 컨페티 + 빅토리 사운드
- **결과 공유**: 결과 텍스트 클립보드 복사 (장식 박스 + 메달 이모지 포맷)
- **다시 시작**: 결과 → idle 상태로 리셋 (참가자/타이틀 유지)
- **사운드 토글**: BGM/스핀 험 음소거
- **데이터 영속화**: 참가자 명단 + 타이틀 localStorage 저장, URL 파라미터(`?names=...&title=...`)로도 초기화 가능

---

## 2. 필수 데이터 요소

### 랜딩
- 로고 영상
- "Loading..." 텍스트

### 브랜드 블록 (전 페이지 좌상단 고정)
- 브랜드 로고 이미지
- 태그라인: "DRIVEN BY PHYSICS — THE EXPERIENCE OF RANDOM SELECTION THROUGH VISUAL COLLISION."

### 인풋 패널
- TITLE 라벨 + 입력값 (placeholder: "TITLE" / 경고 시 "PLEASE ENTER TITLE")
- PARTICIPANTS 라벨
- 현재 참가자 수 뱃지 (예: `12`)
- 참가자 이름 입력창 (placeholder: "Participant Name")
- 참가자 리스트 행 단위 요소:
  - **컬러 닷** (배정된 네온 색)
  - **이름**
  - **삭제(×) 버튼** (idle 전용, 탈락자는 숨김)
  - **순위 태그** (배틀 후 탈락 시 `1st/2nd/3rd/Nth` 표시)
- 리스트 컬럼 규칙: 10명 단위로 좌→우 컬럼 확장 (최대 3컬럼)

### 게임 HUD
- 타이틀 텍스트 (인풋의 타이틀 미러링)
- 잔여 인원 상태 바: `N REMAINING`
- 카운트다운: `3 / 2 / 1 / GO!`
- 타이머: `XX.Xs` (10초 미만 시 urgent 컬러)
- 일시정지 인디케이터: 일시정지 바 아이콘 + `PAUSED` + `CLICK TO RESUME`
- 코너 로스터(우측 하단): 참가자 리스트 미러 + `N / 30` 카운트 + "PARTICIPANTS" 라벨

### 결과 오버레이
- `WINNER` 라벨
- 우승자 이름 (대형 텍스트)
- 우승자 3D 팽이 영역
- 순위 리스트 (2위~): `2nd/3rd/Nth` + 참가자 이름

---

## 3. 사용자 액션

### 인풋 페이지
- **TITLE 입력창**: 텍스트 입력 (실시간 HUD 동기화 + 저장)
- **PARTICIPANT 입력창**: 텍스트 입력 후 Enter → 추가 / 다중 값 페이스트 시 일괄 등록
- **참가자 삭제(×) 버튼**: idle 상태에서 해당 참가자 제거
- **SHUFFLE 버튼**: 순서 및 색상 무작위 재배정
- **사운드 토글 버튼** (스피커 아이콘): BGM/효과음 음소거 토글 (muted 상태 시각 표시 필요)
- **START GAME 버튼**: 배틀 시작 (2명 미만 또는 타이틀 미입력 시 비활성/검증 실패)

### 게임 페이지
- **3D 캔버스 클릭**: 일시정지 ↔ 재개 토글
- **기어 아이콘 버튼** (#popup-toggle): 인풋 팝업 오픈 (자동 pause)
- **닫기(×) 버튼 / 백드롭 클릭 / ESC 키**: 인풋 팝업 닫기 (자동 resume)

### 결과 페이지
- **COPY RESULTS 버튼**: 결과 텍스트 클립보드 복사 (클릭 시 1.5초간 "COPIED!" 피드백)
- **PLAY AGAIN 버튼**: 참가자/타이틀 유지한 채 idle 상태로 리셋

### 전역 키보드
- **Escape**: 인풋 팝업 닫기

---

### 디자이너 참고용 상태 매트릭스

| 상태(phase) | 인풋 패널 | 코너 로스터 | 타이머/상태바 | 결과 오버레이 |
|---|---|---|---|---|
| idle | 표시 | 숨김 | 숨김 | 숨김 |
| intro / countdown / battle | 숨김 (기어로 모달 호출 가능) | 표시 | 표시 | 숨김 |
| result | 숨김 | 표시 (참고용) | 숨김 | 표시 |

| UI 요소 | 활성 조건 |
|---|---|
| START GAME | 참가자 ≥ 2 AND 타이틀 입력 AND phase=idle |
| 삭제(×) 버튼 | phase=idle AND 미탈락 |
| SHUFFLE | phase=idle AND 참가자 ≥ 2 |
| 기어(설정) 버튼 | 배틀 중 모달 호출 (idle/result에서는 동작은 가능하나 pause 로직 미적용) |
